<h1> Comment Project :  <%= @project.name %> </h1>


<%= form_for @comment, :html => { :class => "form_for"} do |f| %>

<% if @comment.errors.any? %>
    <h2><%= pluralize(@comment.errors.count, "error") %> prohibited this comment from being saved:</h2>

    <ul>
    <% @comment.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
<% end %>

<%= f.label :content %>
<%= f.text_field :content, required: true, class: 'input_form_for' %>
<br>
<%= f.label :user_id %>
<%= f.text_field :user_id, required: true, class: 'input_form_for' %>
<br>
<%= f.submit  class: 'submit_form_for' %>
<% end %>
